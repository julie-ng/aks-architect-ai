# Database
export DATABASE_URL=

# Configuration
export REFORMULATION_TEMPERATURE=0.1
export OPENAPI_DOCS_ENABLED=true

# [Search] - Ranking
export RETRIEVAL_TOP_K=5          # results returned (fetches top_k * 3 candidates to re-rank)
export PRIORITY_BOOST_WEIGHT=0.1  # source-priority weight in the re-rank

# [Provider]
# LLM Options for Retrieval
# - see `.env.provider.aws`
# - see `.env.provider.ollama`
export EMBEDDING_PROVIDER=
export EMBEDDING_MODEL=
export EMBEDDING_VECTOR_DIM= # depends on model (Titan=1024, nomic=768)
export REFORMULATION_PROVIDER=
export REFORMULATION_MODEL=
