services:
  qdrant:
    image: qdrant/qdrant:v1 # use v1-unprivileged in deployments
    ports:
      - "6333:6333" # REST API + dashboard UI
      - "6334:6334" # gRPC
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data:
